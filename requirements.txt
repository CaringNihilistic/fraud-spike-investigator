# Pinned to the versions this project was built and verified against.
# Reproducibility is a judging concern: an unpinned build that drifts and
# produces different metrics than the README claims is worse than no README.

# --- model selection compares all three GBDTs; all are required to reproduce
# --- artifacts_out/model_selection.csv, even though XGBoost is the winner.
lightgbm==4.6.0
xgboost==3.2.0
catboost==1.2.10

scikit-learn==1.8.0
pandas==3.0.1
numpy==2.3.5
joblib==1.5.3

# --- P2 investigator (LangGraph + Claude Haiku) ---
langgraph==1.2.2
anthropic==0.111.0

# --- P3 serving + dashboard. React is VENDORED (src/serve/static/vendor/),
# --- so there is no Node/npm dependency here on purpose.
fastapi==0.136.3
uvicorn==0.48.0

# --- tests (httpx is required by fastapi.testclient) ---
pytest==9.0.3
httpx==0.28.1
