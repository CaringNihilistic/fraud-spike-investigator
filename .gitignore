# --- secrets: never commit ---
.env
.env.*
*.pem
*.key
credentials.json
secrets.*

# --- generated output: reproducible from `python -m src.models.train` ---
artifacts_out/
data/
# ...but the A/B/C eval progression is a submission exhibit, not scratch output.
!artifacts_out/
artifacts_out/*
!artifacts_out/eval_runs/
# The model-selection DECISION is a record, not scratch output: it is what
# train.py, ablation.py and run_demo.py read to know the winner is XGBoost.
# Without it a clean clone silently falls back to a different library and
# stops matching the README - which is exactly what broke the first deploy.
!artifacts_out/model_selection_decision.json
# CatBoost writes training telemetry to CWD during model selection.
catboost_info/

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- editors / os ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# NOTE: src/serve/static/vendor/ is deliberately NOT ignored. Those three
# files (react, react-dom, htm) are what make the demo build-free and
# offline-safe - ignoring them would break `python run_demo.py` on a clean
# clone, which is the single most important thing a judge will run.
